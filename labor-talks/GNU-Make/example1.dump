# GNU Make 3.81
# Copyright (C) 2006  Free Software Foundation, Inc.
# This is free software; see the source for copying conditions.
# There is NO warranty; not even for MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

# This program built for i686-pc-linux-gnu

# »Make«-Datenbank; erstellt am: Fri Nov 14 14:45:41 2008

# Variablen

# automatisch
<D = $(patsubst %/,%,$(dir $<))
# automatisch
?F = $(notdir $?)
# Standard
CWEAVE = cweave
# automatisch
?D = $(patsubst %/,%,$(dir $?))
# Umgebung
VMHANDLE = blackdown-jdk-1.4.2
# automatisch
@D = $(patsubst %/,%,$(dir $@))
# automatisch
@F = $(notdir $@)
# Makefile
CURDIR := /home/bg/vortraege/GNU-Make
# Makefile
SHELL = /bin/sh
# Standard
RM = rm -f
# Umgebung
GDM_LANG = de_DE.utf8
# Umgebung
_ = /usr/bin/make
# Standard
PREPROCESS.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -F
# Standard
LINK.o = $(CC) $(LDFLAGS) $(TARGET_ARCH)
# Standard
OUTPUT_OPTION = -o $@
# Umgebung
OPENGL_PROFILE = xorg-x11
# Standard
COMPILE.cpp = $(COMPILE.cc)
# Makefile (from `example1.make', line 1)
MAKEFILE_LIST :=  example1.make
# Umgebung
JAVA_HOME = /home/bg/.gentoo/java-config-2/current-user-vm
# Standard
LINK.p = $(PC) $(PFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# Umgebung
XDG_DATA_DIRS = /usr/share:/usr/kde/3.5/share:/usr/local/share
# Umgebung
USB_DEVFS_PATH = /dev/bus/usb
# Umgebung
GENERATION = 2
# Umgebung
SANE_CONFIG_DIR = /etc/sane.d
# Umgebung
INFOPATH = /usr/share/info:/usr/share/binutils-data/i686-pc-linux-gnu/2.16.1/info:/usr/share/gcc-data/i686-pc-linux-gnu/4.3.1/info:/usr/share/info/emacs-21:/usr/share/gnat-gpl-data/i686-pc-linux-gnu/4.1/info
# Standard
CHECKOUT,v = +$(if $(wildcard $@),,$(CO) $(COFLAGS) $< $@)
# Umgebung
LESSOPEN = |lesspipe.sh %s
# Umgebung
QMAKESPEC = linux-g++
# Standard
CPP = $(CC) -E
# Standard
LINK.cc = $(CXX) $(CXXFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# Umgebung
SGML_CATALOG_FILES = /etc/sgml/sgml-docbook.cat:/etc/sgml/openjade-1.3.2.cat:/etc/sgml/xml-docbook-4.4.cat:/etc/sgml/sgml-ent.cat:/etc/sgml/xml-simple-docbook-1.0.cat:/etc/sgml/xml-simple-docbook-4.1.2.4.cat:/etc/sgml/xml-docbook-4.1.2.cat:/etc/sgml/sgml-docbook-3.0.cat:/etc/sgml/sgml-docbook-3.1.cat:/etc/sgml/sgml-docbook-4.0.cat:/etc/sgml/sgml-docbook-4.1.cat:/etc/sgml/sgml-lite.cat:/etc/sgml/dsssl-docbook-stylesheets.cat
# Umgebung
PATH = /usr/local/bin:/usr/bin:/bin:/usr/gnat/bin:/opt/bin:/usr/i686-pc-linux-gnu/avr/gcc-bin/4.3.1:/usr/i686-pc-linux-gnu/gcc-bin/4.3.1:/usr/i386-pc-linux-gnu/gcc-bin/3.4.4:/opt/ICAClient:/opt/blackdown-jdk-1.4.2.03/bin:/opt/blackdown-jdk-1.4.2.03/jre/bin:/usr/kde/3.5/bin:/usr/qt/3/bin:/usr/i686-pc-linux-gnu/gnat-gpl-bin/4.1:/usr/libexec/gnat-gpl/i686-pc-linux-gnu/4.1:/usr/lib/subversion/bin:/usr/games/bin
# Standard
LD = ld
# Standard
TEXI2DVI = texi2dvi
# Standard
YACC = yacc
# Umgebung
ICAROOT = /opt/ICAClient
# Standard
COMPILE.mod = $(M2C) $(M2FLAGS) $(MODFLAGS) $(TARGET_ARCH)
# Standard
ARFLAGS = rv
# Standard
LINK.r = $(FC) $(FFLAGS) $(RFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# Umgebung
WINDOWID = 12627398
# Standard
COMPILE.f = $(FC) $(FFLAGS) $(TARGET_ARCH) -c
# Umgebung
RUBYOPT = -rauto_gem
# Umgebung
CPATH = /usr/include/libffi
# Standard
LINT.c = $(LINT) $(LINTFLAGS) $(CPPFLAGS) $(TARGET_ARCH)
# Standard
LINT = lint
# Standard
YACC.y = $(YACC) $(YFLAGS)
# Standard
AR = ar
# Umgebung
ADA_OBJECTS_PATH = /usr/lib/gnat-gpl/i686-pc-linux-gnu/4.1/adalib
# Standard
.FEATURES := target-specific order-only second-expansion else-if archives jobserver check-symlink
# Standard
TANGLE = tangle
# Umgebung
LS_COLORS = no=00:fi=00:di=01;34:ln=01;36:pi=40;33:so=01;35:do=01;35:bd=40;33;01:cd=40;33;01:or=01;05;37;41:mi=01;05;37;41:su=37;41:sg=30;43:tw=30;42:ow=34;42:st=37;44:ex=01;32:*.tar=01;31:*.tgz=01;31:*.svgz=01;31:*.arj=01;31:*.taz=01;31:*.lzh=01;31:*.lzma=01;31:*.zip=01;31:*.z=01;31:*.Z=01;31:*.dz=01;31:*.gz=01;31:*.bz2=01;31:*.bz=01;31:*.tbz2=01;31:*.tz=01;31:*.deb=01;31:*.rpm=01;31:*.jar=01;31:*.rar=01;31:*.ace=01;31:*.zoo=01;31:*.cpio=01;31:*.7z=01;31:*.rz=01;31:*.jpg=01;35:*.jpeg=01;35:*.gif=01;35:*.bmp=01;35:*.pbm=01;35:*.pgm=01;35:*.ppm=01;35:*.tga=01;35:*.xbm=01;35:*.xpm=01;35:*.tif=01;35:*.tiff=01;35:*.png=01;35:*.svg=01;35:*.mng=01;35:*.pcx=01;35:*.mov=01;35:*.mpg=01;35:*.mpeg=01;35:*.m2v=01;35:*.mkv=01;35:*.ogm=01;35:*.mp4=01;35:*.m4v=01;35:*.mp4v=01;35:*.vob=01;35:*.qt=01;35:*.nuv=01;35:*.wmv=01;35:*.asf=01;35:*.rm=01;35:*.rmvb=01;35:*.flc=01;35:*.avi=01;35:*.fli=01;35:*.gl=01;35:*.dl=01;35:*.xcf=01;35:*.xwd=01;35:*.yuv=01;35:*.pdf=00;32:*.ps=00;32:*.txt=00;32:*.patch=00;32:*.diff=00;32:*.log=00;32:*.tex=00;32:*.doc=00;32:*.aac=00;36:*.au=00;36:*.flac=00;36:*.mid=00;36:*.midi=00;36:*.mka=00;36:*.mp3=00;36:*.mpc=00;36:*.ogg=00;36:*.ra=00;36:*.wav=00;36:
# Umgebung
SSH_AUTH_SOCK = /tmp/ssh-jbHDEF4526/agent.4526
# Standard
GET = get
# Umgebung
JDK_HOME = /home/bg/.gentoo/java-config-2/current-user-vm
# automatisch
%F = $(notdir $%)
# Umgebung
KDEDIRS = /usr:/usr/local:/usr/kde/3.5
# Umgebung
DISPLAY = :0.0
# Standard
COMPILE.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# Standard
CTANGLE = ctangle
# Standard
.LIBPATTERNS = lib%.so lib%.a
# Umgebung
ADA_INCLUDE_PATH = /usr/lib/gnat-gpl/i686-pc-linux-gnu/4.1/adainclude
# Standard
LINK.C = $(LINK.cc)
# Umgebung
PWD = /home/bg/vortraege/GNU-Make
# Standard
LINK.S = $(CC) $(ASFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_MACH)
# Umgebung
ANT_HOME = /usr/share/ant
# Standard
PREPROCESS.r = $(FC) $(FFLAGS) $(RFLAGS) $(TARGET_ARCH) -F
# Umgebung
SSH_AGENT_PID = 4527
# Umgebung
PKG_CONFIG_PATH = /usr/qt/3/lib/pkgconfig
# Standard
LINK.c = $(CC) $(CFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# Umgebung
MANPATH = /home/bg/.gentoo/java-config-2/current-user-vm/man:/usr/local/share/man:/usr/share/man:/usr/share/binutils-data/i686-pc-linux-gnu/2.16.1/man:/usr/share/gcc-data/i686-pc-linux-gnu/4.3.1/man:/opt/blackdown-jdk-1.4.2.03/man:/etc/java-config/system-vm/man/:/usr/kde/3.5/share/man:/usr/qt/3/doc/man:/usr/share/gnat-gpl-data/i686-pc-linux-gnu/4.1/man:/opt/opera/share/man
# Standard
LINK.s = $(CC) $(ASFLAGS) $(LDFLAGS) $(TARGET_MACH)
# Umgebung
HOME = /home/bg
# Umgebung
LOGNAME = bg
# Umgebung
TEXINPUTS = /usr/lib/ocaml/ocamldoc:
# Standard
CO = co
# automatisch
^D = $(patsubst %/,%,$(dir $^))
# Umgebung
DISTCC_VERBOSE = 0
# Umgebung
GCC_SPECS = 
# Umgebung
MAKELEVEL := 0
# Umgebung
COLORTERM = gnome-terminal
# Standard
MAKE = $(MAKE_COMMAND)
# Umgebung
SHLVL = 4
# Umgebung
CONFIG_PROTECT_MASK = /etc/env.d/java/ /etc/udev/rules.d /etc/fonts/fonts.conf /etc/gconf /etc/terminfo /etc/ca-certificates.conf /etc/texmf/web2c /etc/revdep-rebuild
# Standard
AS = as
# Standard
PREPROCESS.S = $(CC) -E $(CPPFLAGS)
# Umgebung
GUILE_LOAD_PATH = /usr/share/guile/1.8
# Standard
COMPILE.p = $(PC) $(PFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# Standard
MAKE_VERSION := 3.81
# Umgebung
USER = bg
# Standard
FC = f77
# Makefile
.DEFAULT_GOAL := all
# Umgebung
CLASSPATH = .
# Umgebung
LESS = -R -M --shift 5
# automatisch
%D = $(patsubst %/,%,$(dir $%))
# Standard
WEAVE = weave
# Standard
MAKE_COMMAND := make
# Standard
LINK.cpp = $(LINK.cc)
# Standard
F77 = $(FC)
# Umgebung
OLDPWD = /home/bg
# Standard
.VARIABLES := 
# Standard
PC = pc
# Umgebung
CVS_RSH = ssh
# automatisch
*F = $(notdir $*)
# Umgebung
FLTK_DOCDIR = /usr/share/doc/fltk-1.1.7-r2/html
# Standard
COMPILE.def = $(M2C) $(M2FLAGS) $(DEFFLAGS) $(TARGET_ARCH)
# Umgebung
PYTHONPATH = /usr/lib/portage/pym
# Standard
LEX = lex
# Makefile
MAKEFLAGS = qpn
# Umgebung
MFLAGS = -qpn
# automatisch
*D = $(patsubst %/,%,$(dir $*))
# Standard
LEX.l = $(LEX) $(LFLAGS) -t
# automatisch
+D = $(patsubst %/,%,$(dir $+))
# Standard
COMPILE.r = $(FC) $(FFLAGS) $(RFLAGS) $(TARGET_ARCH) -c
# automatisch
+F = $(notdir $+)
# Standard
M2C = m2c
# Umgebung
GDK_USE_XFT = 1
# Standard
CC = cc
# Standard
MAKEFILES := 
# Umgebung
TMAKEPATH = /usr/lib/tmake/linux-g++
# Standard
COMPILE.cc = $(CXX) $(CXXFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# automatisch
<F = $(notdir $<)
# Standard
CXX = g++
# Standard
COFLAGS = 
# Umgebung
EDITOR = /usr/bin/vim
# Umgebung
PAGER = /usr/bin/less
# Umgebung
LC_ALL = de_DE.utf8
# Umgebung
PRELINK_PATH_MASK = /usr/lib/gstreamer-0.8:/opt/eagle-5.2.0
# Standard
COMPILE.C = $(COMPILE.cc)
# automatisch
^F = $(notdir $^)
# Standard
COMPILE.S = $(CC) $(ASFLAGS) $(CPPFLAGS) $(TARGET_MACH) -c
# Standard
LINK.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# Standard
SUFFIXES := .out .a .ln .o .c .cc .C .cpp .p .f .F .r .y .l .s .S .mod .sym .def .h .info .dvi .tex .texinfo .texi .txinfo .w .ch .web .sh .elc .el
# Standard
COMPILE.c = $(CC) $(CFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# Umgebung
DCCC_PATH = /usr/lib/distcc/bin
# Standard
COMPILE.s = $(AS) $(ASFLAGS) $(TARGET_MACH)
# Standard
.INCLUDE_DIRS = /usr/include /usr/include
# Umgebung
CONFIG_PROTECT = /usr/kde/3.5/share/config /usr/kde/3.5/env /usr/kde/3.5/shutdown /usr/share/config
# Umgebung
QTDIR = /usr/qt/3
# Standard
MAKEINFO = makeinfo
# Umgebung
JAVAC = /home/bg/.gentoo/java-config-2/current-user-vm/bin/javac
# Standard
TEX = tex
# Umgebung
LANG = de_DE.utf8
# Umgebung
TERM = xterm
# Standard
F77FLAGS = $(FFLAGS)
# Standard
LINK.f = $(FC) $(FFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# Umgebung
DISTCC_LOG = 
# variable set hash-table stats:
# Load=146/1024=14%, Rehash=0, Collisions=13/173=8%

# Pattern-specific Variable Values

# Keine musterspezifischen Variablenwerte.

# Verzeichnisse

# SCCS: Konnte den Status nicht feststellen.
# . (Gerät 65026, I-Knoten (inode) 646893): 51 Dateien, 37 Unmöglichkeiten.
# RCS: Konnte den Status nicht feststellen.

# 51 Dateien, 37 Unmöglichkeiten in 3 Verzeichnissen.

# Implicit Rules

%.out:

%.a:

%.ln:

%.o:

%: %.o
#  Auszuführende Kommandos (eingebaut):
	$(LINK.o) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.c:

%: %.c
#  Auszuführende Kommandos (eingebaut):
	$(LINK.c) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.ln: %.c
#  Auszuführende Kommandos (eingebaut):
	$(LINT.c) -C$* $<

%.o: %.c
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.c) $(OUTPUT_OPTION) $<

%.cc:

%: %.cc
#  Auszuführende Kommandos (eingebaut):
	$(LINK.cc) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.cc
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.cc) $(OUTPUT_OPTION) $<

%.C:

%: %.C
#  Auszuführende Kommandos (eingebaut):
	$(LINK.C) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.C
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.C) $(OUTPUT_OPTION) $<

%.cpp:

%: %.cpp
#  Auszuführende Kommandos (eingebaut):
	$(LINK.cpp) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.cpp
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.cpp) $(OUTPUT_OPTION) $<

%.p:

%: %.p
#  Auszuführende Kommandos (eingebaut):
	$(LINK.p) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.p
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.p) $(OUTPUT_OPTION) $<

%.f:

%: %.f
#  Auszuführende Kommandos (eingebaut):
	$(LINK.f) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.f
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.f) $(OUTPUT_OPTION) $<

%.F:

%: %.F
#  Auszuführende Kommandos (eingebaut):
	$(LINK.F) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.F
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.F) $(OUTPUT_OPTION) $<

%.f: %.F
#  Auszuführende Kommandos (eingebaut):
	$(PREPROCESS.F) $(OUTPUT_OPTION) $<

%.r:

%: %.r
#  Auszuführende Kommandos (eingebaut):
	$(LINK.r) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.r
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.r) $(OUTPUT_OPTION) $<

%.f: %.r
#  Auszuführende Kommandos (eingebaut):
	$(PREPROCESS.r) $(OUTPUT_OPTION) $<

%.y:

%.ln: %.y
#  Auszuführende Kommandos (eingebaut):
	$(YACC.y) $< 
	$(LINT.c) -C$* y.tab.c 
	$(RM) y.tab.c

%.c: %.y
#  Auszuführende Kommandos (eingebaut):
	$(YACC.y) $< 
	mv -f y.tab.c $@

%.l:

%.ln: %.l
#  Auszuführende Kommandos (eingebaut):
	@$(RM) $*.c
	$(LEX.l) $< > $*.c
	$(LINT.c) -i $*.c -o $@
	$(RM) $*.c

%.c: %.l
#  Auszuführende Kommandos (eingebaut):
	@$(RM) $@ 
	$(LEX.l) $< > $@

%.r: %.l
#  Auszuführende Kommandos (eingebaut):
	$(LEX.l) $< > $@ 
	mv -f lex.yy.r $@

%.s:

%: %.s
#  Auszuführende Kommandos (eingebaut):
	$(LINK.s) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.s
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.s) -o $@ $<

%.S:

%: %.S
#  Auszuführende Kommandos (eingebaut):
	$(LINK.S) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.S
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.S) -o $@ $<

%.s: %.S
#  Auszuführende Kommandos (eingebaut):
	$(PREPROCESS.S) $< > $@

%.mod:

%: %.mod
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.mod) -o $@ -e $@ $^

%.o: %.mod
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.mod) -o $@ $<

%.sym:

%.def:

%.sym: %.def
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.def) -o $@ $<

%.h:

%.info:

%.dvi:

%.tex:

%.dvi: %.tex
#  Auszuführende Kommandos (eingebaut):
	$(TEX) $<

%.texinfo:

%.info: %.texinfo
#  Auszuführende Kommandos (eingebaut):
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.texinfo
#  Auszuführende Kommandos (eingebaut):
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.texi:

%.info: %.texi
#  Auszuführende Kommandos (eingebaut):
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.texi
#  Auszuführende Kommandos (eingebaut):
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.txinfo:

%.info: %.txinfo
#  Auszuführende Kommandos (eingebaut):
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.txinfo
#  Auszuführende Kommandos (eingebaut):
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.w:

%.c: %.w
#  Auszuführende Kommandos (eingebaut):
	$(CTANGLE) $< - $@

%.tex: %.w
#  Auszuführende Kommandos (eingebaut):
	$(CWEAVE) $< - $@

%.ch:

%.web:

%.p: %.web
#  Auszuführende Kommandos (eingebaut):
	$(TANGLE) $<

%.tex: %.web
#  Auszuführende Kommandos (eingebaut):
	$(WEAVE) $<

%.sh:

%: %.sh
#  Auszuführende Kommandos (eingebaut):
	cat $< >$@ 
	chmod a+x $@

%.elc:

%.el:

(%): %
#  Auszuführende Kommandos (eingebaut):
	$(AR) $(ARFLAGS) $@ $<

%.out: %
#  Auszuführende Kommandos (eingebaut):
	@rm -f $@ 
	cp $< $@

%.c: %.w %.ch
#  Auszuführende Kommandos (eingebaut):
	$(CTANGLE) $^ $@

%.tex: %.w %.ch
#  Auszuführende Kommandos (eingebaut):
	$(CWEAVE) $^ $@

%:: %,v
#  Auszuführende Kommandos (eingebaut):
	$(CHECKOUT,v)

%:: RCS/%,v
#  Auszuführende Kommandos (eingebaut):
	$(CHECKOUT,v)

%:: RCS/%
#  Auszuführende Kommandos (eingebaut):
	$(CHECKOUT,v)

%:: s.%
#  Auszuführende Kommandos (eingebaut):
	$(GET) $(GFLAGS) $(SCCS_OUTPUT_OPTION) $<

%:: SCCS/s.%
#  Auszuführende Kommandos (eingebaut):
	$(GET) $(GFLAGS) $(SCCS_OUTPUT_OPTION) $<

# 86 implizite Regeln, 5 (5,8%) Terminal.

# Dateien

# Dies ist kein Ziel:
.web.p:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(TANGLE) $<

programm: code_a.o code_b.o code_c.o
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Die Datei existiert nicht.
#  Datei wurde aktualisiert.
#  Eine Aktualisierung ist notwendig (-q ist angegeben).
# variable set hash-table stats:
# Load=0/32=0%, Rehash=0, Collisions=0/2=0%
#  Auszuführende Kommandos (aus »example1.make«, Zeile 4):
	gcc -o programm code_a.o code_b.o code_c.o
	

# Dies ist kein Ziel:
.l.r:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LEX.l) $< > $@ 
	mv -f lex.yy.r $@

# Dies ist kein Ziel:
code_c.c:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.dvi:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.F.o:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.F) $(OUTPUT_OPTION) $<

all: programm
#  Suche nach impliziten Regeln wurde durchgeführt.
#  Die Datei existiert nicht.
#  Datei wurde aktualisiert.
#  Eine Aktualisierung ist notwendig (-q ist angegeben).
# variable set hash-table stats:
# Load=0/32=0%, Rehash=0, Collisions=0/2=0%

# Dies ist kein Ziel:
.y.ln:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(YACC.y) $< 
	$(LINT.c) -C$* y.tab.c 
	$(RM) y.tab.c

# Dies ist kein Ziel:
.o:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LINK.o) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Dies ist kein Ziel:
.y:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

code_a.o: code_a.c code_a.h
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Ersetzung für implizites/statisches Muster: »code_a«
#  Die Datei existiert nicht.
#  Datei wurde aktualisiert.
#  Eine Aktualisierung ist notwendig (-q ist angegeben).
# automatisch
# @ := code_a.o
# automatisch
# % := 
# automatisch
# * := code_a
# automatisch
# + := code_a.c code_a.h
# automatisch
# | := 
# automatisch
# < := code_a.c
# automatisch
# ^ := code_a.c code_a.h
# automatisch
# ? := code_a.c code_a.h
# variable set hash-table stats:
# Load=8/32=25%, Rehash=0, Collisions=1/10=10%
#  Auszuführende Kommandos (aus »example1.make«, Zeile 7):
	gcc -c -o code_a.o code_a.c
	

# Dies ist kein Ziel:
.def.sym:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.def) -o $@ $<

# Dies ist kein Ziel:
.p.o:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.p) $(OUTPUT_OPTION) $<

# Dies ist kein Ziel:
.p:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LINK.p) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Dies ist kein Ziel:
.txinfo.dvi:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# Dies ist kein Ziel:
.a:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.l.ln:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	@$(RM) $*.c
	$(LEX.l) $< > $*.c
	$(LINT.c) -i $*.c -o $@
	$(RM) $*.c

# Dies ist kein Ziel:
code_c.h:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.w.c:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(CTANGLE) $< - $@

# Dies ist kein Ziel:
.texi.dvi:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# Dies ist kein Ziel:
.sh:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	cat $< >$@ 
	chmod a+x $@

# Dies ist kein Ziel:
.cc:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LINK.cc) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Dies ist kein Ziel:
.cc.o:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.cc) $(OUTPUT_OPTION) $<

# Dies ist kein Ziel:
.def:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.SUFFIXES: .out .a .ln .o .c .cc .C .cpp .p .f .F .r .y .l .s .S .mod .sym .def .h .info .dvi .tex .texinfo .texi .txinfo .w .ch .web .sh .elc .el
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.c.o:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.c) $(OUTPUT_OPTION) $<

# Dies ist kein Ziel:
.r.o:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.r) $(OUTPUT_OPTION) $<

# Dies ist kein Ziel:
.r:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LINK.r) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Dies ist kein Ziel:
code_b.h:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

code_b.o: code_b.c code_b.h code_a.h code_c.h
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (aus »example1.make«, Zeile 9):
	gcc -c -o code_b.o code_b.c
	

# Dies ist kein Ziel:
.info:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.elc:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.l.c:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	@$(RM) $@ 
	$(LEX.l) $< > $@

# Dies ist kein Ziel:
.out:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.C:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LINK.C) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Dies ist kein Ziel:
.r.f:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(PREPROCESS.r) $(OUTPUT_OPTION) $<

# Dies ist kein Ziel:
.S:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LINK.S) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Dies ist kein Ziel:
.texinfo.info:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# Dies ist kein Ziel:
.c:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LINK.c) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Dies ist kein Ziel:
.w.tex:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(CWEAVE) $< - $@

# Dies ist kein Ziel:
.c.ln:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LINT.c) -C$* $<

# Dies ist kein Ziel:
.s.o:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.s) -o $@ $<

# Dies ist kein Ziel:
.s:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LINK.s) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Dies ist kein Ziel:
code_a.c:
#  Suche nach impliziten Regeln wurde durchgeführt.
#  Zuletzt geändert 2008-11-14 14:45:20
#  Datei wurde aktualisiert.
#  Erfolgreich aktualisiert.
# variable set hash-table stats:
# Load=0/32=0%, Rehash=0, Collisions=0/0=0%

# Dies ist kein Ziel:
.texinfo.dvi:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# Dies ist kein Ziel:
.el:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.texinfo:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.y.c:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(YACC.y) $< 
	mv -f y.tab.c $@

# Dies ist kein Ziel:
.web.tex:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(WEAVE) $<

# Dies ist kein Ziel:
.texi.info:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# Dies ist kein Ziel:
.DEFAULT:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.h:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
code_a.h:
#  Suche nach impliziten Regeln wurde durchgeführt.
#  Zuletzt geändert 2008-11-14 14:45:20
#  Datei wurde aktualisiert.
#  Erfolgreich aktualisiert.
# variable set hash-table stats:
# Load=0/32=0%, Rehash=0, Collisions=0/0=0%

# Dies ist kein Ziel:
.tex.dvi:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(TEX) $<

# Dies ist kein Ziel:
.cpp.o:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.cpp) $(OUTPUT_OPTION) $<

# Dies ist kein Ziel:
.cpp:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LINK.cpp) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Dies ist kein Ziel:
.C.o:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.C) $(OUTPUT_OPTION) $<

# Dies ist kein Ziel:
.ln:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
code_b.c:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.texi:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.txinfo:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

code_c.o: code_c.c code_c.h
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (aus »example1.make«, Zeile 11):
	gcc -c -o code_c.o code_c.c
	

# Dies ist kein Ziel:
.tex:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.txinfo.info:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# Dies ist kein Ziel:
.ch:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
example1.make:
#  Suche nach impliziten Regeln wurde durchgeführt.
#  Zuletzt geändert 2008-11-13 23:43:50
#  Datei wurde aktualisiert.
#  Erfolgreich aktualisiert.
# variable set hash-table stats:
# Load=0/32=0%, Rehash=0, Collisions=0/0=0%

# Dies ist kein Ziel:
.S.s:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(PREPROCESS.S) $< > $@

# Dies ist kein Ziel:
.mod:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.mod) -o $@ -e $@ $^

# Dies ist kein Ziel:
.mod.o:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.mod) -o $@ $<

# Dies ist kein Ziel:
.F.f:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(PREPROCESS.F) $(OUTPUT_OPTION) $<

# Dies ist kein Ziel:
.w:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.S.o:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.S) -o $@ $<

# Dies ist kein Ziel:
.l:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.F:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LINK.F) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Dies ist kein Ziel:
.web:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.sym:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.

# Dies ist kein Ziel:
.f:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(LINK.f) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Dies ist kein Ziel:
.f.o:
#  Suche nach impliziten Regeln wurde nicht durchgeführt.
#  Zeit der letzten Änderung wurde nicht überprüft.
#  Datei wurde nicht aktualisiert.
#  Auszuführende Kommandos (eingebaut):
	$(COMPILE.f) $(OUTPUT_OPTION) $<

# files hash-table stats:
# Load=78/1024=8%, Rehash=0, Collisions=336/1436=23%
# VPATH-Suchpfade

# Keine »vpath«-Suchpfade.

# Kein allgemeiner Suchpfad (Variable »VPATH«).

# # of strings in strcache: 1
# # of strcache buffers: 1
# strcache size: total = 4096 / max = 4096 / min = 4096 / avg = 4096
# strcache free: total = 4082 / max = 4082 / min = 4082 / avg = 4082

# »Make«-Datenbank; beendet am: Fri Nov 14 14:45:41 2008

