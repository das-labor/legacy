#ifndef FILEPARSER_H
#define FILEPARSER_H

// The format is explaint in "anims/sample.prn"

// Plays the animation file with the filename filename. (.prn)
void playAnimFile(char *filename);

// Plays the a playlist with animation files. (.apl)
void playPlaylist(char *filename);	

#endif
